export default class SentryObjects {
    constructor(sentryid, name, minimumYears, maximumYears){
        this.sentryid = sentryid
        this.name = name
        this.minimumYears = minimumYears
        this.maximumYears = maximumYears
    }
}